apiVersion: v1
kind: Service
metadata:
  name: bookmyshow-svc
  namespace: bookmyshow-ns
spec:
  selector:
    app: bookmyshow
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: LoadBalancer
